# Ruff configuration for boote's helper scripts — https://docs.astral.sh/ruff/
line-length = 100
target-version = "py39"

[lint]
# pycodestyle (logical errors), Pyflakes, and import sorting. Line length is
# intentionally not enforced (E501) to keep grounded prompt strings readable.
select = ["E4", "E7", "E9", "F", "I"]
